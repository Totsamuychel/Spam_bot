# Telegram Multi-Account Message Sender

Безопасная рассылка сообщений с мультиаккаунтной поддержкой, обработкой блокировок, и удобной заменой аккаунтов через Telegram бот.

## Возможности

- 🔄 Мультиаккаунтная поддержка с автоматическим переключением
- 🛡️ Защита от блокировок с контролем лимитов Telegram
- ⚡ Умная очередь сообщений с распределением нагрузки
- 📊 Статистика отправки и мониторинг аккаунтов
- 🔧 Горячая замена заблокированных аккаунтов

## Структура проекта

```
sender/
├── src/
│   ├── account_manager.py      # Управление аккаунтами и их статусами
│   ├── rate_limiter.py         # Контроль лимитов и защита от блокировок
│   ├── message_queue.py        # Очередь сообщений и логика распределения
│   └── sender.py               # Основная логика отправки сообщений
├── sessions/                   # Сессии аккаунтов
│   ├── account1.session
│   └── account2.session
├── data/                       # Данные для рассылки
│   └── messages_data.json
├── requirements.txt            # Зависимости проекта
└── main.py                     # Точка входа в приложение
```

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/Totsamuychel/Spam_bot.git
cd sender
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Добавьте файлы сессий в папку `sessions/`

4. Настройте данные для рассылки в `data/messages_data.json`

## Использование

### Запуск бота
```bash
python main.py
```

### Основные команды

- **Добавить аккаунт** - Загрузка новой сессии аккаунта
- **Получить активный аккаунт** - Выбор следующего доступного аккаунта
- **Пометить как заблокированный** - Исключение аккаунта из ротации
- **Статистика** - Просмотр статуса всех аккаунтов и метрик

## Как это работает

### 1. Управление аккаунтами (`account_manager.py`)
- Загрузка и хранение сессий всех аккаунтов
- Автоматический пропуск заблокированных аккаунтов
- Горячее добавление/удаление аккаунтов
- Мониторинг статуса каждого аккаунта

### 2. Контроль лимитов (`rate_limiter.py`)
Система защиты от превышения лимитов Telegram:
- Отслеживание отправленных сообщений по времени
- Проверка лимитов перед каждой отправкой
- Автоматическое замедление при приближении к лимиту
- Увеличение паузы при обнаружении блокировки

### 3. Очередь сообщений (`message_queue.py`)
- Получение количества сообщений для рассылки
- Построение оптимальной очереди распределения
- Балансировка нагрузки между аккаунтами
- Обработка данных из JSON файлов

### 4. Отправка сообщений (`sender.py`)
- Основная логика отправки через Telegram API
- Чтение данных пользователей из JSON
- Отправка по ID или номеру телефона
- Обработка ошибок и исключений

## Лимиты Telegram

Система учитывает следующие ограничения:
- Максимальное количество сообщений в минуту
- Ограничения на новые чаты
- Защита от спам-фильтров
- Автоматические паузы при блокировках

## Формат данных

### messages_data.json
```json
{
  "message": "Текст сообщения для рассылки",
  "recipients": [
    {
      "user_id": 123456789,
      "username": "@username"
    },
    {
      "phone": "+1234567890"
    }
  ]
}
```

## Безопасность

- Все сессии хранятся локально
- Автоматическое управление лимитами API
- Защита от массовых блокировок
- Логирование всех операций

## Требования

- Python 3.7+
- Активные Telegram аккаунты
- API ключи Telegram

## Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь в валидности сессий
3. Проверьте лимиты аккаунтов
4. Обновите заблокированные аккаунты

---

**Внимание**: Используйте бота в соответствии с правилами Telegram и применимым законодательством.